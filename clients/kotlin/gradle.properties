org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# The published coordinates. Bump VERSION_NAME, then `./gradlew publishToMavenCentral`.
GROUP=com.vapolia.analytics
VERSION_NAME=1.0.0
